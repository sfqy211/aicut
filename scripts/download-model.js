console.log("AICut uses faster-whisper model names by default.");
console.log("For CPU-first V1, keep AICUT_ASR_MODEL=small in .env.");
console.log("The model will be downloaded by faster-whisper on first ASR startup.");
